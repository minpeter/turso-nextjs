export const metadata = {
  title: `Admin Panel | Telos`,
  description: `Admin Panel for Telos`,
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
